# Documentation Audit

The repository contains 53 Markdown/Mermaid documentation files. Current README links now point to implemented security, partner, upgrade, operations, staging, architecture, OpenAPI, and ADR artifacts. The repository includes `docs/openapi.yaml`, `docs/architecture.md`, `docs/adr/`, `.env.example`, and the staging acceptance record. Historic audit reports are retained as decision records and explicitly marked superseded where applicable.
