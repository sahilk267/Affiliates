# 🎨 UI/UX Rules

1. Use standard layout system (grid/flex) consistently.
2. Ensure mobile responsiveness and accessibility.
3. Style components from shared theme/config.
4. Avoid repetition — reuse components.
5. Test UI changes across screen sizes.
