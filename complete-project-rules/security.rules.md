# 🔐 Security Rules

1. Sanitize and validate all external input.
2. Store secrets (API keys, DB credentials) in .env files.
3. Use hashed passwords and encrypted tokens.
4. Always protect endpoints with auth checks.
5. Apply least-privilege access control in DB and APIs.
